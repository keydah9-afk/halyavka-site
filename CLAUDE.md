# halyavka.net — ГДЗ-сайт (Astro, статика)

- Запуск: `pnpm dev --port 4399` (см. `.claude/launch.json`). Билд: `pnpm build`.
- Стек: Astro + Tailwind (`darkMode: 'class'`), View Transitions (`astro:page-load` в скриптах компонентов).
- Бренд: червоний `#C1121F`, темно-синій `#003049`, кремовий `#FDF0D5`; тёмная тема через `:global(.dark)`.
- Классическое ГДЗ: ответы = webp-картинки в `public/images/<klas>/<book>/`, данные книги в `src/data/<author>-<subject>-<klas>.ts`, страница в `src/pages/<klas>-klas/gdz-.../index.astro`, просмотрщик — `ExerciseGallery.astro`.
  Структуру (§, розділи, состав вправ) для новых книг брать из HTML-файла `зміст`, который
  пользователь кладёт в папку книги, — это точные данные, навигатор от NotebookLM для них НЕ нужен.
  Парсеры-генераторы data-файлов: `scripts/gdz/` (там же README и что делать, когда доедут
  недостающие блоки). Обязательная проверка перед сдачей — плоский список кнопок против реальных
  файлов в `public`: 0 missing / 0 extra / 0 dup. Имена файлов приводить к схеме СТАРОЙ страницы,
  иначе ломаются внешние deep-link-и `/…/#123`. Трекер всей пакетной работы — `PERENOS-STATUS.md`.

## Интерактивное ГДЗ (новый формат, без картинок)

Первая книга: **Хімія 8 клас, Григорович** — `/8-klas/gdz-himiya-grygorovych/`.
Решения хранятся структурированным текстом и рендерятся в два режима:
«⚡ Списати» (ответ как в тетради + копирование) и «🧠 Зрозуміти» (пошаговый разбор, виджеты, прогресс в localStorage).

- `src/data/gdz-solutions.ts` — общие типы (Solution, SolutionStep, виджеты molar/choice, TrainerSubstance).
- `src/data/grygorovych-himiya-8.ts` — контент: **розділ 1 (§1–§7)** + **§8–§12 і Навчальне
  дослідження № 1** — 171 завдання (вправи 1–134, 23 «Поміркуйте», 14 пунктів НД № 1).
  Нумерація наскрізна: §1 — 1–6, §2 — 7–13, §3 — 14–26, §4 — 27–35, §5 — 36–40,
  §6 — 41–52, §7 — 53–63, §8 — 64–72, §9 — 73–94, §10 — 95–117, §11 — 118–124,
  §12 — 125–134.
  Усі розрахункові відповіді звірено з «Відповідями на розрахункові задачі» (стор. 319).
  Розходження одне — вправа 27б: підручник друкує w(Cl) = 25,4 %, правильно 25,5 %
  (у ГДЗ дано правильне значення з поясненням).
  Там, де ключа немає, числа перевірені окремо: вправа 71 (графік тиску кисню) — за
  міжнародною стандартною атмосферою; вправа 93 (розчинність кисню) — за даними §9
  (10,2 мл/л за 0 °С, 6,7 мл/л за 20 °С → 14,6 і 9,6 мг/л).
  **Розбір «Зрозуміти» у §1–§7 підтягнуто до планки §9–§12 (30.08.2026):** 6,3 → 8,0 кроку
  на завдання, виджет — у 40 % завдань (было 15 %). Закрыты две дыры: вправа 42 (не было
  `parts` вообще — теперь 3 части по 4 шага) и pom-49 (не было `intro`). Массивы `answer`
  не тронуты. Самые «жирные» приросты — там, где параграф ровно про виджет: §3 (Mr) 5,3 → 7,5
  и §6 (молярна маса) 6,1 → 8,3.
- **Навчальні дослідження** оформляються як звичайний `paragraph`, але з id-префіксом
  (`nd1-…`) і власними `caption`. Структура топіків, що себе виправдала: «Підготовка
  до роботи» (мета/обладнання, ризики, «Поміркуйте»-гіпотези) → «Хід роботи та
  спостереження» (протокол: хід — спостереження — рівняння — висновок) → «Підбиття
  підсумків роботи» → «Рефлексуємо» (`collapsed: true`).
- `src/components/SolutionGallery.astro` — TOC + просмотрщик двух режимов (hash deep-link `#42`, поиск, ← →, прогресс `gdz-done:<storageKey>` в localStorage, режим в `halyavka-gdz-mode`).
  У виджета `choice` **все три текстовых поля** (`prompt`, `html` варианта, `note`) рендерятся
  через `set:html` — можно писать `<sub>`, `<sup>`, `<b>`. Раньше `prompt` и `note` шли как
  обычный текст, и три виджета показывали теги буквально; починено 30.08.2026.
- **Вся книга пока на ОДНОМ URL** `/8-klas/gdz-himiya-grygorovych/` — так решил
  пользователь, и это подкреплено конкурентами: shkola.in.ua и gdzonline.net по
  этому же Григоровичу держат книгу на одной странице (у них ответы — issuu-iframe
  и JPG-сканы в лайтбоксе, поэтому HTML 70–120 КБ; у нас текст, поэтому 1,3 МБ,
  но грузимся мы за 0,28 с против их 4–8 с). Смысл одного URL: ученик никуда не
  переходит, все поведенческие и ссылочные сигналы копятся на одной странице.
  Механика вынесения на будущее уже готова и НЕ ТРОГАЕТСЯ без нужды:
  `ROZDILY_ON_MAIN` в данных = сколько первых разделов рендерится на главной
  (`mainSections`); остальные (`rozdilyOffMain`) уезжают на `/rozdil-<n>/`,
  которые генерит `[rozdil].astro`. Сейчас = 2, то есть вся книга на главной
  и маршрут не даёт ни одной страницы — так и задумано.
  **Число можно только УМЕНЬШАТЬ** (вынести следующие разделы), увеличивать нельзя:
  уже опубликованный отдельно раздел обратно не переезжает, иначе ломаются ссылки.
  Разделы 3–4 показаны внизу главной серыми карточками из `rozdilyPlanned`.
  Прогресс общий на книгу (`storageKey="himiya8"`), счётчик «Твій прогрес: n із N розібрано»
  считает пересечение с текущей страницей — работает и на одной, и на нескольких.
  На главной есть скрипт-редирект по `itemRozdil`: когда раздел уедет на свой URL,
  старые ссылки `/gdz-himiya-grygorovych/#305` не сломаются. Пока карта пуста —
  скрипт молчит.
  Крошки рисует `PageLayout` сам — свой `<Breadcrumbs>` на страницу НЕ ставить
  (будет второй BreadcrumbList); человекочитаемое имя слага живёт в `segmentLabels`
  внутри `Breadcrumbs.astro`.
  **Растяжка:** вернуться к вопросу, когда главная перевалит за ~2,5 МБ или
  ~25 тыс. DOM-узлов (ориентировочно §16–§17). Розділ 2 — это §8–§23, вправи
  64–299, то есть 60 % книги; законченным он потянет ~3 МБ.
- `src/components/ChemTrainer.astro` — тренажёр «задача с новыми числами» (n=m/M, m=n·M), пул веществ из данных книги.
- Виджет `molar` умеет два режима: по умолчанию `M(...) = ... г/моль`, а с `symbol: 'M<sub>r</sub>'`
  и `unit: ''` — безразмерная `Mr(...) = ...` (нужно для §3–§4). Числа выводятся с десятичной
  запятой; для одноатомных веществ (Cu) слагаемое не дублируется.
- У `Solution` есть опциональный `caption` — подпись в просмотрщике. По умолчанию:
  числовой label → «Вправа 42», нечисловой → сам label («с. 45»). Задавать явно нужно
  там, где «Вправа …» читается плохо (пункты навчальних досліджень).

Вся разметка решений рендерится на билде (SEO); JS только переключает классы.

### С чего начать следующую сессию

**Следующий блок — § 13 «Молярний об'єм газів» (стор. 108–115), вправи 135–146
(12 штук) + 1 «Поміркуйте» (стор. 109); сразу за ним — Навчальне дослідження № 2
«Визначення молярного об'єму газів» (стор. 116–119) с 2 «Поміркуйте» и протоколом
(id-префикс `nd2-`, свои `caption` — как делали для НД № 1).** Это первый после §7
насквозь расчётный параграф: закон Авогадро, Vm = 22,4 л/моль (н. у.).
В ключе (стор. 319) есть 137 (а) 44,8 л; б) 11,2 л; в) 5,6 л; г) 64,96 л; д) 94,08 л),
138 (найбільша — із SO₂), 139 (25,5 г), 140 (а) 112 л; б) 224 л), 141 (в 1,25 раза),
143 (300 л). Остальные (135, 136, 142, 144, 145, 146) — качественные либо без ключа.
Задание 145 — классическая ловушка «1 моль води = 22,4 л?» (нет: вода не газ),
146 — доказательства по закону Авогадро.
Дальше по порядку: §14 «Взаємодія оксидів із водою» + НД № 3 и НД № 4, и так до §35.
Розділ 2 уже заведён как вторая секция в `sections` — новые параграфы дописывать в неё;
вставка идёт перед закрывающим `    ],` / `  },` / `];` в конце файла.

Что стоит помнить по §10 (пригодится дальше): «трикутник вогню» — сквозная идея всего
раздела, на неё ссылаются задания 102, 105, 107, 110, 112, 113, 117; в §10 нет ни одного
задания с картинкой-обязаловкой, но 107 и 112 без описания рисунка непонятны — описание
вшито прямо в `task` (стр. 91 и 92 книги = картинки 94 и 95, смещение image = page + 3).

Что стоит помнить по §11: параграф качественный, единственная расчётная задача — 121
(21 м³ : 20 л/год = 1050 годин ≈ 44 доби), она сверена с ключом. Сквозная идея §11 —
разница «кисень (речовина O₂)» vs «Оксиген (елемент)»: на ней целиком построено задание
120, и на него ссылается 123. Задания 120 и 123 требуют рисунка/таблицы — их содержимое
вшито прямо в `task` (мал. 11.2 описан словами: красные стрелки — расход, зелёные —
фотосинтез; таблица 123 переписана строкой через `;`). Числа, посчитанные независимо
(ключа нет): суммы по таблице 123 — природные 190–290, антропогенные 22–33, всего
212–323 млрд т/год; образование 3,6·10¹¹ т = 90 % → 400 млрд т/год; запас кислорода
в атмосфере ≈ 1,2·10¹⁵ т (23 % от массы атмосферы 5·10¹⁵ т); 122 — 2C₈H₁₈ + 25O₂ =
16CO₂ + 18H₂O, на 1 г бензина ≈ 3,5 г кислорода.

Что стоит помнить по §12: в ключе (стор. 319) для §12 нет НИ ОДНОГО ответа — все числа
считались независимо и сверены с карточками свойств из §9 (кисень) и §12 (озон):
густина 2,14 : 1,43 ≈ 1,5 — и это ровно M(O₃) : M(O₂) = 48 : 32 (хороший приём: густини
газів за однакових умов відносяться як молярні маси); розчинність 49,4 : 10,2 ≈ 4,8;
Δt кип. = 71 °С; в 132 — отношения к порогу нюха 0,01: ГДК 0,1 → 10, ГДК 0,16 → 16,
смертельна доза 4,8 → 480, а 4,8 : 0,16 = 30; в pom-104 — оценка роста дозы УФ
в 100–200 раз (1 % и 0,5 % от полного потока).
Два задания требуют картинок — содержимое вшито прямо в `task`: 131 (график «Уміст
озону на різних висотах», стор. 106 = картинка 109 — оси переставлены, висота по
вертикали, тиск озону по горизонтали; пик ≈ 32–33 мПа на 22–23 км) и 132 (таблица ГДК).
Термина «алотропія» в учебнике нет вообще (проверено grep) — в pom-102 он введён
как бонус, с аналогией алмаз/графит (она в книге появляется позже).
Приём, который пригодится для листовок и планов: пустая строка `''` в массиве `answer`
рендерится как видимый отступ (`.sol-ans-line { min-height: 24px }`) и сохраняется
при копировании — так свёрстано задание 133.

Рабочий процесс, который себя оправдал:

1. Исходники у пользователя в `~/Developer/PROMPTS/гдз хімія 8 клас григорович/`:
   учебник-PDF (**с текстовым слоем**), папка «учебник в картинках» и старое ГДЗ конкурента
   (только как справка — решения писать свои).
2. `pdftotext -layout <учебник>.pdf book.txt` в скретчпад, дальше `grep -n` по маркерам
   «Робота з інформацією», «Розуміння явищ», «Поміркуйте» и по номерам вправ — это
   намного быстрее, чем читать страницы-картинки.
3. Ответы на расчётные задачи — в тексте около строки «ВІДПОВІДІ НА РОЗРАХУНКОВІ ЗАДАЧІ»
   (стор. 319). Прогонять их скриптом, а не глазами; там, где ключа нет, проверять числа
   независимо (стандартная атмосфера, справочные t кип. и т. п.).
4. Фрагмент параграфа писать в отдельный `.ts`-файл heredoc'ом с `<<'TSEOF'` (кавычки
   обязательны, иначе bash съест `$` и backticks), проверять синтаксис через
   `node -e "new Function('return ['+src+']')()"` и только потом сплайсить Python-скриптом.
5. Перед сдачей: `npx astro check` (13 ошибок в репо — преэкзистентные, в других файлах),
   `pnpm build` (129 страниц, ~150 с), проверка живого рендера и консоли.
6. Коммитить только по просьбе пользователя.
